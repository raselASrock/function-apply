const a ={name:`Laptop`, color:`black`, price: 12000};
const b = {name:`Monitor`, color:`gray`, price: 5000};
const c = {name:`Mobile`, color:`black`, price: 2500};

const total = a.price + b.price + c.price;
// console.log(total);
const laptopPrice = a.price;
const monitorPrice = b.price;
const mobilePrice = c.price;

const totalPrice = laptopPrice + monitorPrice + mobilePrice ;
console.log(totalPrice);
